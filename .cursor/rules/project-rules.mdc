---
description: Basic rules for AI agents working on this project
alwaysApply: true
---

# Project Rules for AI Agents

## Code Style

- Use TypeScript strictly; avoid `any` unless necessary
- Prefer functional components and hooks in React
- Use meaningful names; keep functions small and focused

## Commit Messages

Use [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/):

```
<type>[optional scope]: <description>
```

- **feat:** new feature (MINOR)
- **fix:** bug fix (PATCH)
- **docs:** documentation only
- **style:** formatting, no code change
- **refactor:** code change that neither fixes nor adds a feature
- **perf:** performance improvement
- **test:** adding or updating tests
- **chore:** build, CI, tooling
- **BREAKING CHANGE:** use `!` after type/scope or footer (MAJOR)

Example: `feat(auth): add login form`

## Versioning & Changelog

- Use [Semantic Versioning](https://semver.org/): MAJOR.MINOR.PATCH
- Version lives in `package.json`; shown in footer via Vite `define`
- When releasing: bump `package.json` version, then add a `CHANGELOG.md` entry

### Changelog format

- Follow [Keep a Changelog](https://keepachangelog.com/)
- File: `CHANGELOG.md` at project root
- Sections: `### Added`, `### Changed`, `### Deprecated`, `### Removed`, `### Fixed`, `### Security`
- Entry format: `## [X.Y.Z] - YYYY-MM-DD`
- Add new entries at the top (above previous releases)

### Version bump rules

- **PATCH** — bug fixes, minor tweaks
- **MINOR** — new features, non-breaking changes
- **MAJOR** — breaking changes (use `BREAKING CHANGE` in commit)

## Editing

- Prefer editing existing files over creating new ones
- Preserve existing formatting and indentation
- Run linter after changes; fix any new issues

## Project Context

- React 19 + TypeScript + Vite app
- WoW raid cooldown tracker: characters, dungeons, per-character–dungeon toggles
- Data in localStorage; dungeons list starts empty by default
- `data/dungeons.ts` — DungeonList template (WoW WotLK raids, Russian names)
- App version from `package.json` via Vite `define` (`__APP_VERSION__` in `vite-env.d.ts`)
- Follow existing patterns; check README before adding dependencies

## Key Behaviors

- **Add from template** — loads DungeonList into table (empty state)
- **Reset dungeons** — clears all cooldown toggles only (keeps dungeon list)
- **Delete all** — removes all dungeons and toggles
- **Reset per character** — clears toggles for that character only

## Component Structure

- **kebab-case** for component dirs (e.g. `character-form/`)
- Each component: `index.tsx`, `types.ts`, `styles.css`
- State centralized in `useRaidTracker`; components receive handlers via props

## Project Structure

```
src/
├── components/   # character-form, dungeon-form, dungeon-table
├── hooks/         # useRaidTracker
├── types/         # characters, dungeons
├── data/          # dungeons.ts (DungeonList template)
├── assets/        # class icons
├── storage.ts     # localStorage load/save
├── uuid.ts        # generateUUID
└── vite-env.d.ts  # __APP_VERSION__ declaration
```

## Communication

- Be direct and concise
- Prefer answering questions before making edits when unclear
- Explain non-obvious changes briefly
